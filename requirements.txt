Django==3.0.2
# Для работы с изображениями models.ImageField()
Pillow==7.0.0
# Postgres DB
psycopg2==2.8.4
# Пакет для анализа переменнной окружения DATABASE_URL
# и автоматически преобразовывает ее в желаемый формат конфигурации Django
dj_database_url==0.5.0
# http сервер для WSGI приложений
gunicorn==20.0.4
# Обслуживание статических файлов для веб-приложений Python
whitenoise==5.0.1
# SFTP storages
django-storages==1.8
paramiko==2.10.1
# Webdav storages
django-webdav-storage==1.0.0
requests==2.22.0
# Визуальный редактор текста
django-ckeditor==5.9.0
# Панель отладки Django
django-debug-toolbar==2.2